describe('first', () => {
  it('should first', () => {
    expect(true).toEqual(true);
  });
  it('should second', () => {
    expect(true).toEqual(false);
  });
  xit('skip', () => {});
  it('fouth second', () => {});
});
